use "../model/data";
use "../workspace/data";
use "elfie/prelude/builtin";
use "elfie/system/path";
use "elfie/system/time" as Time;
use "elfie/target/target";

enum Reason: `Why a unit needs generating` {
  fresh = `no output has been generated for it`,
  changed = `its source differs from what its output was generated from`,
  dependency = `the interface of a unit it depends on differs from the one its output was generated against`,
  requested = `the caller asked for it regardless`,
}

d Marker: `One place where generated code says where it came from` {
  $outputLine: `The line of the output file that carries the marker, counting from 1` = number;
  $file: `The source path the marker names, relative to [[Workspace.root]]` = string;
  $entity: `The name the marker spells, when it names an entity: the identifier, or the owner's identifier, a dot, and a member's name; undefined when it names a line` = string | undefined;
  $line: `The source line: as spelled, or derived from [[Marker.entity]] as the first line of that entity's declaration` = number;
  $column: `The source column, counting from 0; undefined when the marker names a whole line or an entity` = number | undefined;
  @acceptanceCriteria
    .add({
      behavior = `A marker is written in the output as a line comment of the target's language reading @lfy, a space, the source path relative to [[Workspace.root]], a colon, and then either a line (optionally a colon and a column) or the name of an entity of that file`,
    })
    .add({
      behavior = `Text is read as a marker only when what follows @lfy and the space is a path ending in .lfy, a colon, and a number or an identifier path; anything else after @lfy is prose`,
    })
    .add({
      behavior = `The compiler is asked to write names, never lines, so that its markers survive edits that move lines; every line in a source map is derived from the model, never copied from the compiler`,
    });
}

d SourceMap: `What one output file was generated from, recorded mechanically at acceptance` {
  $target: `The identifier of the target it was generated for` = string;
  $output: `The output file's path, relative to [[Workspace.root]]` = string;
  $source: `The source file's path, relative to [[Workspace.root]]` = string;
  $hash: `SHA-256 of the source file's bytes when the output was accepted, as lowercase hex` = string;
  $signature: `SHA-256 of the unit's interface text, as interfaceOf spells it, when the output was accepted` = string;
  $dependencies: `For each dependency's source path, the [[SourceMap.signature]] the output was generated against` = object;
  $generated: `When the output was accepted: [[Time.Instant.rfc3339]] of [[Time.now]] at acceptance` = string;
  $markers: `Every [[Marker]] in the output, in output order, with lines derived` = Marker[];
}

d Unit: `The work the compiler does as one piece: one source file for one target` {
  $target: `What it is built for` = Target;
  $file: `The source file` = File;
  $entities: `The entities of the file built for the target, in file order; never an entity of the elfie package` = Entity[];
  $stem: `[[Path.relative]] of [[File.path]] to the directory it was found under, without its extension; the target's guidance spells the output file from it` = string;
  $dependencies: `The units of the same target for the files this file uses, transitively through files that have no unit` = Unit[];
  $outputs: `The outputs the last accepted generation produced, from the target's source maps; empty when none` = SourceMap[];
  $reason: `Why it is planned; undefined when it is up to date` = Reason | undefined;
}

d Batch: `Planned units compiled together in one request, so shared context is sent once` {
  $units: `In plan order; every dependency of a unit is in this batch or an earlier one` = Unit[];
  $identifier: `The stem of the first unit, then the count when there are more, as the batch is named in requests and progress` = string;
}

d Interface: `What a dependent unit may rely on from another unit's output` {
  $unit: `The unit` = Unit;
  $outputs: `The paths of its outputs` = string[];
  $entities: `Its entities: each one's identifier, kind, definition, type, parameters and output as applicable; a type may name an entity of the elfie package by its identifier` = Entity[];
}

d Request: `Everything the compiler is handed to produce the outputs of one batch` {
  $batch: `The batch` = Batch;
  $instructions: `The prompt: what to produce, where, the rules for producing it, what of the standard library is [[builtin]] and never generated, and how to report the outcome` = string;
  $sources: `The text of each unit's source file, by [[File.path]]` = object;
  $previous: `The source text each unit's existing outputs were generated from, by [[File.path]]; absent where unknown` = object;
  $existing: `The current text of each existing output of the batch, by path` = object;
  $interfaces: `One [[Interface]] per dependency outside the batch, in dependency order, each once` = Interface[];
  $guidance: `Every criterion of [[Target.marker]], then of every trait it extends down to [[target]], nearest first, and of every trait applied to it with arguments, each resolved for the marker` = Criterion[];
  $nativeDependencies: `What the generated code may require from the target's ecosystem` = NativeDependency[];
}

d Output: `One file the compiler produced` {
  $path: `Relative to [[Workspace.root]]` = string;
  $text: `Its content` = string;
}

enum OutcomeKind: `How one run of the compiler ended` {
  accepted = `every unit of the batch was accepted`,
  rejected = `an output failed acceptance`,
  blocked = `the compiler reported it could not proceed`,
  clarification = `the compiler asked a question only a person can answer`,
  failed = `the compiler command could not run, or exited without producing anything`,
}

d Outcome: `What one run of the compiler came to, read mechanically from its report and the verdicts` {
  $kind: `How it ended` = OutcomeKind;
  $message: `The compiler's reason or question, or the first problem; empty when accepted` = string;
  $verdicts: `One [[Verdict]] per unit of the batch, in batch order; empty when the compiler never got that far` = Verdict[];
}

d Verdict: `Whether outputs are accepted for one unit` {
  $accepted: `Whether every check passed` = boolean;
  $problems: `What failed and why, one per failure; empty when accepted` = string[];
  $sourceMaps: `One per accepted output; empty when rejected` = SourceMap[];
  $outputs: `Each accepted output with its markers normalized to the name form, for the caller to write back; empty when rejected` = Output[];
}

d Plan: `Every unit of a workspace, which need generating, and how they are batched` {
  $workspace: `The workspace planned` = Workspace;
  $units: `Every unit of every target, each after its dependencies` = Unit[];
  $batches: `The planned units grouped for compilation, each batch after the batches holding its dependencies` = Batch[];
}
